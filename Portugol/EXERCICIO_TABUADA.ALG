Algoritmo "exercicio_tabuada"

Var

      n, i, mult : inteiro

Inicio

      escreva("Diseja a tabuada para qual valor? ")
      leia(n)
      
      para i de 1 ate 10 faca
           mult <- n * i
           escreval(n:1, " x " ,i:1, " = ", mult)
      fimpara
      

Fimalgoritmo